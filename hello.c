#include<stdio.h>

int main(){

    int x, y, z;
    prinf("Enter X and Y Values to add two numers: ");
    scanf("%d%d", &x, &y);// i/p x and y values;
    z = x + y;
    prinf("%d + %d = %d", x, y, z);
    prinf("Hello world");


    return 0;

}