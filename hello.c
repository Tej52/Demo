#include<stdio.h>

int main(){

    int x, y, z;
    prinf("Enter X and Y Values to add two numers: ");
    scanf("%d%d", &x, &y);// i/p x and y values;
    z = x + y;
    printf("%d + %d = %d", x, y, z);
    printf("Hello world");


    return 0;

}
